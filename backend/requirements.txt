# Lovli backend — runtime dependencies for Railway / production.
# Versions are pinned where stability matters; ranges allow safe patches elsewhere.

# Web framework
fastapi==0.110.1
uvicorn[standard]==0.25.0
python-multipart>=0.0.9

# Database
motor==3.3.1
pymongo==4.5.0

# Validation
pydantic>=2.6.4
email-validator>=2.2.0

# Auth
pyjwt>=2.10.1
passlib[bcrypt]>=1.7.4
bcrypt==4.1.3

# HTTP (Google OAuth code exchange)
httpx>=0.27.0

# LLM (Claude via Anthropic SDK)
anthropic==0.99.0

# Config + utilities
python-dotenv>=1.0.1
tzdata>=2024.2
