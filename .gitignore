# --- Secrets ---
.env
.env.local
.env.development
.env.production
.env.*.local

# --- Node / web ---
node_modules/
dist/
build/
.next/
.vercel/
.cache/

# --- Expo / RN ---
.expo/
.expo-shared/
.metro-cache/
.metro-health-check*
web-build/
# Expo prebuild output — regenerated by `npx expo prebuild`. Tracking it would
# add ~3GB (Pods/ + build/) for 29 source files that app.json already describes.
mobile/ios/
mobile/android/
*.jks
*.p8
*.p12
*.key
*.mobileprovision

# --- Python ---
__pycache__/
*.py[cod]
.pytest_cache/
.venv/
venv/
*.egg-info/

# --- OS / IDE ---
.DS_Store
Thumbs.db
.idea/
.vscode/
*.log
coverage/

# --- Test artifacts ---
/test_reports/
/tmp/
*.local

.env.*
*.env
# Always commit example envs (negations must come AFTER the ignore patterns)
!.env.example
!**/.env.example
credentials.json
*.pem
.credentials
